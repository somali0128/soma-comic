# Stickman Soma

个人站点（React + Tailwind CSS）。

## 脚本

- `npm start` — 本地开发
- `npm run build` — 生产构建
- `npm test` — 测试
- `npm run deploy` — 部署到 `gh-pages`（需配置 `homepage`）

## 技术栈

React、React Router、Tailwind CSS（Create React App 脚本构建）。
